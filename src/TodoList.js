import React from "react";
import {useState} from "react";
import Card from "react-bootstrap/Card";

function TodoList(){
    const[task, setTasks] = useState("Click here to edit text")
        //states are anything that can be updated. I want the card to be able to be updated

   
}
 export default TodoList;